### Why OOPs?

Problems:In the Identical approach we need a lot of independent variable  to create an object.
         No Authority to over data means we need to repeated initialization of attribute of an object.
OOP- Object oriented programming solve all these problem we discuss above how ?  oops, is mimic of the real world problems.

## Classes: classes are the blueprint of real world entities.

Syntax: 
        `class student {
                          }`

## Objects:objects are NonPrimitive datatype in java used to present real word entities inside a class.

Syntax:
`Student s1=new Student();`

### Hot points :

class not contain any memory before object creation

**s1** is a reference variable of object that store in **stack** or point to **new Student ()** inside of **heap memory**

s1,s2 all references of an objects store address of an object not value  
every object have some State and behavior(in general methods in java but in oops we call behavior)
check image for the visuals:
[Object in memory](img1.png)
source code :